/* jshint -W117, -W030 */
describe('terms routes', function() {
  describe('state', function() {
   /* var view = 'app/terms/faq.html';

    beforeEach(function() {
      module('app.terms', bard.fakeToastr);
      bard.inject('$httpBackend', '$location', '$rootScope', '$state', '$templateCache');
    });

    beforeEach(function() {
      $templateCache.put(view, '');
    });

    it('should map state admin to url /terms ', function() {
      expect($state.href('faq', {})).to.equal('/faq');
    });

    it('should map /faq route to faq View template', function() {
      expect($state.get('faq').templateUrl).to.equal(view);
    });

    it('of faq should work with $state.go', function() {
      $state.go('faq');
      $rootScope.$apply();
      expect($state.is('faq'));
    });*/
  });
});
